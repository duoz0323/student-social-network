// Tâm mặc định dùng canonical demo seed Chợ Bình Trị Đông để môi trường local mở đúng vùng có dữ liệu.
export const DISCOVERY_MAP_CONFIG = Object.freeze({
  defaultCenter: Object.freeze({ lat: 10.75865, lng: 106.60485 }),
  defaultZoom: 14,
  userLocationZoom: 16,
  locationPostsLimit: 10,
});
